services:
  sms-gateway:
    build:
      context: .
      args:
        VERSION: ${IMAGE_VERSION:-local}
    image: ${IMAGE_REPOSITORY:-sms-gateway}:${IMAGE_VERSION:-local}
    pull_policy: ${PULL_POLICY:-never}
    container_name: sms-gateway
    restart: unless-stopped
    ports:
      - "${PORT:-5174}:${PORT:-5174}"
    environment:
      PORT: ${PORT:-5174}
      DB_DRIVER: ${DB_DRIVER:-sqlite}
      DB_DSN: ${DB_DSN:-/opt/sms-gateway/sms-gateway.db}
      CONFIG_FILE: ${CONFIG_FILE:-/opt/sms-gateway/sms-gateway.conf}
      DEVICE_PATH: ${DEVICE_PATH:-/dev/ttyUSB0}
      BAUD_RATE: ${BAUD_RATE:-9600}
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET must be set in the environment or .env}
      DEV_MODE: ${DEV_MODE:-false}
    volumes:
      - sms-gateway-data:/opt/sms-gateway
      # Bind the host /dev tree so ttyUSB devices remain visible even if the modem
      # enumerates as a different ttyUSB number after reconnects.
      - /dev:/dev
    device_cgroup_rules:
      - "c 188:* rwm"

volumes:
  sms-gateway-data: